export type MenuItem = {
  text: string;
  link: string;
};

export type IconType = 'github' | 'twitter' | 'linkedin';

export type SocialItem = {
  name: IconType;
  title: string;
  link: string;
};
